<div class="jumbotron p-3 p-md-5 text-white rounded bg-dark">
  <div class="col-md-6 px-0">
    <h1 class="display-4 font-italic"><%= item.title %></h1>
    <p class="lead my-3"><%= truncate(item.body, :length => 100) %></p>
    <p class="lead mb-0"><%= link_to 'Continue Reading...', blog_path(item.id), class: 'text-white font-weight-bold' %></p>
  </div>
</div>